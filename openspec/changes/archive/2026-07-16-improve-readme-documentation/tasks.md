## 1. Verify the Documentation Baseline

- [x] 1.1 Reconcile the public command inventory, flags, stdout behavior, repository requirements, config locations, credential scopes, output paths, and exclusions against the current Cobra help, implementation, and all six canonical OpenSpec capabilities; record and resolve any contradiction before drafting user guidance. Verification: Immediate — the inventory accounts for `config`, `agent skill`, work items, pull requests, wikis, and pipelines without an unsupported claim.
  - Result: Live help from a temporary current binary, the six canonical specifications, and the relevant config/export/agent implementations establish the public command surface and boundaries. Reconciliation resolved four documentation traps: the legacy handover's old agent-generation non-goal is stale; wiki bundle leaves are encoded so users must trust stdout and `index.json` instead of constructing the leaf from the spec placeholder; exact PAT scope names are unspecified except pipeline `vso.build`; and generated-skill workflow advice is not automatic CLI behavior. Direct remote `go install ...@latest` remains unverified and will not be documented.
- [x] 1.2 Smoke-test the clone-based Go install and build paths with isolated temporary `GOBIN`, `GOCACHE`, and `GOMODCACHE` locations, then run the produced binary with `--help`; test a direct remote module install only if it will be documented. Verification: Immediate — the chosen copied installation command produces a runnable `adomi` binary, or the artifacts are back-propagated before documentation continues.
  - Result: `go install ./cmd/adomi` succeeded with isolated temporary Go caches and binary directory after the expected sandbox-network retry, and the installed binary's `adomi --help` exited successfully. `go build -o /tmp/adomi-doc-smoke-adomi ./cmd/adomi` and the built binary's help also succeeded. Documentation will use only these clone-based Go paths.

## 2. Rebuild the GitHub Landing Page

- [x] 2.1 Rewrite `README.md` with a concise outcome-focused introduction, intended audience, repository-local agent-context workflow, and explicit distinction between read-only context gathering and user-authorized narrow maintenance. Verification: Immediate — a section audit satisfies both public-README scenarios in `repository-documentation` without relying on the linked guides.
  - Result: The README introduction and `Why Adomi` section now explain the repository-local agent workflow, intended developer/agent audience, data-only automation model, OS-keyring credential handling, and explicit write boundary before any linked guide is required.
- [x] 2.2 Add a compact capability map covering config/profiles, agent skill generation, work item context and comments, PR context and maintenance, wiki context, and pipeline status, pairing each workflow with its canonical command, result, and material safety boundary. Verification: Immediate — every implemented-workflow scenario maps to current CLI help and no unsupported workflow appears.
  - Result: The `What it can do` table covers all six user-facing workflow areas with canonical commands, outputs, and verified exclusions; comparison with live help and the canonical specs found no unsupported workflow claim.
- [x] 2.3 Add the minimal verified installation and quick-start path, links to both user guides, help/discovery guidance, and a concise contributor section with the repository's actual format, test, and build commands. Verification: Deferred — final install smoke, link audit, command audit, tests, and build in section 4 necessarily prove this chunk.
  - Result: Deferred gates 4.1–4.3 passed: fresh-clone install/build and help smokes succeeded, all 14 relative documentation links resolved, contributor formatting/tests/build passed, and strict OpenSpec validation was clean.

## 3. Create the User Guides

- [x] 3.1 Create `docs/getting-started.md` with prerequisites, the verified installation path, binary/PATH verification, repository and global config setup, YAML activation, profile selection, secure PAT login, optional global/project agent-skill setup, first work item or PR fetch, output inspection, and common setup corrections. Verification: Immediate — a clean walkthrough covers every installation and first-use scenario using placeholders and no inline PAT value.
  - Result: The new guide walks from prerequisites through both verified binary paths, fully-commented YAML activation, scope/profile/keyring setup, all four agent-skill targets, two first-fetch options, local output inspection, `.gitignore`, and the material config/keyring/redirect/proxy corrections. A focused secret-pattern scan found no embedded PAT or authorization value.
- [x] 3.2 Replace `docs/adomi_azure_devops_handover.md` with `docs/azure-devops.md`, carrying forward only current user-relevant material and documenting work item, PR, wiki, pipeline, config/profile, authentication, proxy, output, stdout, and conservative write behavior from current contracts. Verification: Immediate — each detailed command and limitation is traced to CLI help, canonical specs, or implementation, and the obsolete handover path is absent.
  - Result: `docs/azure-devops.md` now traces the full user surface to current help/spec/source contracts, including exact stdout forms, bundle contents, profile/keyring resolution, proxy and redirect behavior, every supported write, and explicit exclusions. It tells wiki users to trust the printed encoded path, avoids unverified non-pipeline scope names, and the obsolete handover file is absent. Focused scans found no embedded credential value.
- [x] 3.3 Add navigation between the README and both guides, remove duplicated exhaustive details from the README, and check that terminology, command names, placeholders, output paths, and credential guidance are consistent across all three documents. Verification: Deferred — the final relative-link and cross-document command audits in section 4 necessarily prove this chunk.
  - Result: Deferred gates passed: all 14 exact-case relative links across the three documents resolve, no section-anchor references are present, representative commands match live help, and focused scans found no stale handover, remote-install, unsupported scope, or literal wiki-leaf claim.

## 4. Review and Verify the Documentation

- [x] 4.1 Execute every documented installation/build form and representative side-effect-free `--help` command from the guides using isolated temporary destinations; confirm the copied commands, flags, and PATH guidance behave as written. Verification: Immediate — all command smokes exit successfully and the installed binary reports Adomi help.
  - Result: A fresh temporary `git clone https://github.com/Digni/adomi.git` completed, isolated `go install ./cmd/adomi` and `go build -o ./bin/adomi ./cmd/adomi` forms succeeded, and both binaries returned root help. Representative help for config, agent skill, ADO, fetch, work item comment, PR ensure/comment, wiki fetch, and pipeline commands all exited 0 with the documented usage.
- [x] 4.2 Run the documented contributor checks, including formatting verification, `go test ./...`, and `go build ./...`, to prove the README's contributor workflow against the full repository. Verification: Immediate — formatting is clean and the full test and build commands pass, or any unrelated baseline blocker is preserved and reported without expanding scope.
  - Result: `gofmt -l cmd internal` returned no files; `go test ./...` passed all six packages; and `go build ./...` passed after the expected outside-sandbox retry for Go's module stat-cache write. The same formatting, six-package test, and full-build gates passed again after the review fix.
- [x] 4.3 Check every repository-local Markdown file link and referenced section anchor in `README.md`, `docs/getting-started.md`, and `docs/azure-devops.md`; run `git diff --check` and `openspec validate improve-readme-documentation --strict`. Verification: Immediate — all links resolve with matching case, the diff check is clean, and strict OpenSpec validation passes.
  - Result: The focused checker verified 14 relative links and zero referenced anchors across the three maintained documents with exact casing; `git diff --check` and `openspec validate improve-readme-documentation --strict` both passed. Both checks passed again after the review fix.
- [x] 4.4 Run the `implementation-review` skill over the completed documentation diff, fix all actionable P0–P2 accuracy, usability, security, or scope findings, and rerun the affected gates from 4.1–4.3. Verification: Immediate — the final review verdict reports no unresolved actionable findings and all rerun gates pass.
  - Result: The initial review found one valid P2 because the README opening could imply pipeline status was exported below `.adomi/context/`. The sentence now separates repository-local work item/PR/wiki exports from compact pipeline JSON; all affected gates passed again, and the follow-up review reported `patch is correct` with 98% confidence and no remaining actionable P0–P2 findings.
