## 1. Lock the redirect failure contract with tests

- [x] 1.1 Add failing `internal/ado` tests that construct the client through `NewHTTPClient`, return representative 301/302/303/307/308 responses with HTML bodies and valid or malformed redirect locations, and assert that the redirect target receives zero requests. Cover a JSON read, a JSON write through `doJSON`, and `Download`; require the original status in each error, forbid JSON-decode/HTML/Location leakage, and require redirected downloads to return no bytes. Verification: `go test -count=1 ./internal/ado -run 'Test(NewHTTPClient|Client).*Redirect'` fails before implementation on followed redirects or malformed-location leakage and passes after the redirect policy/error handling is added.
- [x] 1.2 Add a focused CLI regression test using the production `ado.NewHTTPClient` path for an invalid-token sign-in redirect, asserting non-zero error behavior, empty stdout, a concise status-bearing error, and no request to the sign-in target. Preserve existing direct network-error and stream assertions. Verification: `go test -count=1 ./internal/cli -run 'TestADO.*(InvalidToken|Redirect|NetworkError)'` fails on the new redirect case before implementation and passes afterward.

## 2. Implement fail-closed Azure DevOps redirect handling

- [x] 2.1 Configure `NewHTTPClient` to return `http.ErrUseLastResponse` from `CheckRedirect` and wrap its transport with a narrow guard that removes only unparseable `Location` values from 301/302/303/307/308 responses before Go's redirect parser runs, preserving the existing proxy transport and 60-second timeout. Do not alter redirect policy on arbitrary clients injected into `NewClient`. If implementation evidence shows a supported Azure DevOps endpoint requires redirects, stop and update `proposal.md`, `design.md`, the delta spec, and these tasks before adding an exception. Verification: `go test -count=1 ./internal/ado -run 'TestNewHTTPClient.*(Proxy|Redirect)'` passes, proving proxy validation remains intact and valid, missing, and malformed redirect locations do not trigger a follow-up request or location-leaking transport error.
- [x] 2.2 Add redirect-specific formatting to the shared response error path so 301/302/303/307/308 statuses retain the action/request ID and original status, provide PAT/base-URL guidance, and exclude response HTML and `Location`; keep other 3xx responses on the ordinary status-error path, preserve bounded body context for other non-redirect errors, and preserve clean empty-body formatting. Verification: `go test -count=1 ./internal/ado -run 'Test((NewHTTPClient|Client).*Redirect|Client.*Non2xx|ResponseError)'` passes for valid/missing/malformed redirects, non-followed 3xx responses, direct 401/403/500, empty-body, JSON read/write, and attachment-download cases.
- [x] 2.3 Run the focused CLI regression and correct only propagation or stream-handling gaps exposed by it; do not add command-specific redirect logic when the shared client/error path already satisfies the contract. Verification: `go test -count=1 ./internal/cli -run 'TestADO.*(InvalidToken|Redirect|NetworkError)'` passes with stdout empty and the original redirect status available through the CLI error path.

## 3. Verify unaffected behavior and the complete change

- [x] 3.1 Format changed Go files and run the full affected-package suites, including existing successful JSON, malformed/empty JSON, direct authorization error, proxy, attachment, work item, and pull request tests. Verification: `gofmt -w internal/ado internal/cli` makes no further changes on a second run, and `go test -count=1 ./internal/ado ./internal/cli` passes.
- [x] 3.2 Build and test the entire Go module before completion. Verification: `go test -count=1 ./...` and `go build ./...` both pass.
- [x] 3.3 Validate the OpenSpec change and review the final diff against the approved scope. Verification: `openspec validate handle-invalid-ado-token --strict` passes, `openspec status --change handle-invalid-ado-token` reports all four artifacts complete, and `git diff --check` reports no whitespace errors while the unrelated `azdo-wiki-downloads-and-search` change remains untouched.
- [x] 3.4 Run the required post-implementation multi-perspective review, address any actionable findings, and rerun every affected verification command after review fixes. Verification: the agent review reports no unresolved actionable findings and the commands from tasks 3.1 through 3.3 still pass.
