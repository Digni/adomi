## 1. Increment 1 — PR discovery

- [x] 1.1 Add optional PR page controls and a bounded discovery reader using the existing list client, preserving the default `pr ensure` call. Cover filters on every page, short pages followed by more matches, empty results, duplicate IDs, invalid responses, and ceilings with HTTP fixtures. Verification: Deferred to gate 1.4.
- [x] 1.2 Wire PR list parsing, repository selection, normalized refs, compact projected JSON, namespace/action help, and empty stdout on failure. Cover detached HEAD, explicit repository override, invalid/repeated flags, and help without credentials. Verification: Deferred to gate 1.4.
- [x] 1.3 Update README/reference/generated skill guidance for PR discovery and subsequent `pr fetch`, preserving current governance and host-keyring guidance. Verification: Deferred to gate 1.4 for help/generator tests and gate 6.1 for documentation review.
- [x] 1.4 Run `go test -count=1 ./internal/ado ./internal/cli -run 'PullRequest|PRList|AgentSkill'` and review generated/help examples against the PR discovery spec; expect all new contract checks and existing ensure/governance regressions to pass. Verification: Immediate; record actual command/result below before starting chunk 2.

## 2. Increment 1 — branch-filtered pipeline history

- [x] 2.1 Add optional branch selection through list parsing/read seams and both Build URL builders. Preserve no-branch requests, exact in-progress default, recent count/order/token handling, and current JSON projection; reject source-ref mismatches. Add HTTP-to-CLI fixtures with unrelated recent runs, matching continuation pages, full refs, empty matches, and invalid branch flags. Verification: Deferred to gate 2.3.
- [x] 2.2 Update branch-list help, README/reference, and generated guidance, including project scope and explicit PR merge refs. Verification: Deferred to gate 2.3 for help/generator checks and gate 6.1 for documentation review.
- [x] 2.3 Run `go test -count=1 ./internal/ado ./internal/cli -run 'Pipeline|AgentSkill'`; expect filtered requests on every page and all existing pipeline transport/list/get output regressions to pass. Verification: Immediate; record actual result before chunk 3.

## 3. Increment 1 — work-item discussion exports

- [x] 3.1 Add the separate comment read model and paginated Comments reader with the endpoint-specific read version, ascending/non-deleted selection, metadata preservation, identity validation, opaque tokens, and ceilings. Fetch once per existing tree item before replacing an export; keep comment-create behavior unchanged. Verification: Deferred to gate 3.4.
- [x] 3.2 Add `--include-comments`, per-item JSON/index references, escaped HTML discussion, and comment feedback outside the attachment-count callback. Add fixtures for parent/direct-child coverage, later-page handover text, edits/deletions, empty versus omitted comments, retrieval failure preserving the prior export, default refresh removing stale discussion, and exact existing stdout/attachment counts. Verification: Deferred to gate 3.4.
- [x] 3.3 Update fetch help, README/reference, and generated guidance for opt-in discussion and unchanged output. Verification: Deferred to gate 3.4 for help/generator checks and gate 6.1 for documentation review.
- [x] 3.4 Run `go test -count=1 ./internal/ado ./internal/cli -run 'WorkItem|Fetch|Export|AgentSkill'`; expect new discussion fixtures, default export/attachment regressions, and comment-write regressions to pass. Verification: Immediate; record actual result before increment 2.

## 4. Increment 2 — execution and test readers

- [x] 4.1 Add inspection-specific models/reader seams for the selected Build URI, root/detail timelines, observed attempt references, and failed-task text logs. Reuse existing guarded pipeline transport without changing summary JSON or non-pipeline policy. Cover classic Build without stages, skipped/pending/unknown records, detail-reference deduplication, failed log mapping, absent references, HTTP failures, and fixed budgets. Verification: Deferred to gate 4.3.
- [x] 4.2 Add server-side Build-URI-filtered Test run reads and all-outcome result paging, with separately labeled reported/calculated counts and duplicate/mismatched identity validation. Cover multiple test runs, short/result pages, empty published tests, missing Build URI, Test permission failures, and aggregate request/byte limits. Verification: Deferred to gate 4.3.
- [x] 4.3 Run `go test -count=1 ./internal/ado -run 'Pipeline|Inspection'`; expect full timeline/log/test HTTP fixtures, safe error markers, bounded retrieval, and existing pipeline transport regressions to pass. Verification: Immediate; record actual result before export/CLI integration.

## 5. Increment 2 — inspection bundle and command

- [x] 5.1 Add unique timestamped inspection snapshots, artifact/index layout, observation metadata, relative evidence links, counts, and unfinished-directory cleanup. Verify repeated inspections remain separate, absent collections are explicit, and injected write/network failures preserve older snapshots. Verification: Deferred to gate 5.4.
- [x] 5.2 Wire `pipeline inspect` through command/dependency seams with signed-int32 validation, profile/global/JSON flags, path/compact-JSON output, and stderr-only progress. Add an HTTP-to-CLI-to-filesystem fixture proving which records/logs/tests were exported and that partial retrieval cannot print success. Verification: Deferred to gate 5.4.
- [x] 5.3 Update README/reference, pipeline/help routing, and generated guidance for stage/job/task evidence, failed-task logs, published tests, scopes, limits, snapshot paths, and one-shot/current-attempt limitations; replace obsolete blanket exclusions of pipeline detail. Verification: Deferred to gate 5.4 for executable/help/generator checks and gate 6.1 for documentation review.
- [x] 5.4 Run `go test -count=1 ./internal/ado ./internal/cli -run 'Pipeline|Inspection|AgentSkill'`; expect end-to-end bundle contents and failure cleanup/output tests to pass alongside unchanged list/get contracts. Verification: Immediate; record actual result before final review.

## 6. Review and complete verification

- [x] 6.1 Use `implementation-review` for the completed standard-risk diff, checking query/pagination correctness, default compatibility, export evidence semantics, output/help/docs, and preservation of `enable-complete-pr-flow` governance guidance. Resolve findings without unrelated cleanup. Verification: Immediate; record reviewer findings and any fixes in this artifact.
- [x] 6.2 After review fixes, verify formatting with `gofmt -l` on changed Go files (expect no output), then run `go test -count=1 ./...`, `go vet ./...`, `go build ./...`, `openspec validate --all --strict --no-interactive`, and `git diff --check`. Verification: Immediate; record each actual result, retain unrelated baseline/external blockers, and do not claim full completion if a required gate is unresolved.
- [x] 6.3 Use `runtime-verify` with a temporary built binary and host-capable execution for every `adomi` invocation. On a configured ADO project, read a known branch with `pr list --source <branch> --status all`, `pipeline list --branch <branch> --last 10`, a known discussion via `fetch <id> --include-comments`, and `pipeline inspect <run-id>` on a run with published tests and a failed task where available. Compare identities, task states, at least one retrieved log and test outcome, discussion text, source metadata, and stdout with current ADO UI/read evidence. Verification: Immediate; record exact non-secret commands/IDs and observed artifacts, including unavailable retention/permission/data cases. Do not queue builds or mutate ADO to create fixtures; missing live cases remain explicitly unverified unless accepted from deterministic evidence.
- [x] 6.4 Check this change against the completed predecessor and retain the documented future archive order (`enable-complete-pr-flow` first), without archiving either change as part of implementation. Keep governance/host-execution requirements and this change's tasks/specs aligned with the final implementation, then log the session in the existing Adomi Obsidian project. Verification: Immediate; check requirement compatibility, strict OpenSpec status, and read back the project work note.

## 7. Verification record

Implementation started on 2026-09-07 with user-approved Luna implementation subagents and root verification. During apply, replace each pending result with the actual command/action, expected outcome, actual result, and any blocker before advancing past its gate. Use the existing Go/httptest harness with TDD for behavior changes; no new test framework is required.

| Gate | Expected result | Actual result |
| --- | --- | --- |
| 1.4 PR discovery | Complete bounded discovery, stable output, unchanged PR maintenance | Passed: root host-side focused Go tests, 2026-09-07 |
| 2.3 Branch history | Server-side branch selection on every page; existing modes retained | Passed: root host-side focused Go tests, 2026-09-07 |
| 3.4 Comments | Complete opt-in discussion; default files/output and attachment counts retained | Passed: root host-side focused Go tests and live export, 2026-09-07 |
| 4.3 Inspection readers | Correct finite timeline/log/test reads with existing transport protections | Passed: root host-side focused Go tests, 2026-09-07 |
| 5.4 Inspection export | Coherent snapshot files, truthful counts/scope, no successful partial output | Passed: root host-side focused Go tests and live bundles, 2026-09-07 |
| 6.1 Review | Findings resolved; specs, help, and generated guidance agree | Passed: independent review returned zero findings; root checked results |
| 6.2 Repository checks | Formatting, full tests/vet/build, strict specs, clean diff checks pass | Passed: all required checks, 2026-09-07 |
| 6.3 Runtime | Actual selected ADO evidence matches local output; gaps explicitly recorded | Passed: all four commands via normal host Adomi reads; unavailable independent UI/raw baseline recorded |
| 6.4 Spec/log completion | Predecessor guidance retained and project log read back | Passed: compatibility checked and Adomi project note appended/read back |

Proposal-only verification (2026-09-07): all four artifact groups are complete; strict all-repository OpenSpec validation passed 10/10; capability coverage, preservation of modified scenario sets, and whitespace checks passed. No Go code was changed and no code tests/builds or live ADO calls were run. Implementation gates above are not satisfied by proposal validation.

Implementation evidence:
- Task 1.3: generator contract test failed first on missing PR discovery instructions, then `go test ./internal/cli -run '^TestAgentSkillCreatesGlobalDefaultAdomiSkill$' -count=1` passed after the guidance update. README/reference updated; joint gate 1.4 and final documentation review remain pending.
- Task 1.1: Luna implemented optional page controls and bounded discovery; root read the client/model/helper changes and tests. Luna reports focused PR tests, `go test ./internal/ado -count=1`, and `go vet ./internal/ado` passed. Root joint gate 1.4 remains pending.
- Gate 1.4: root verified dispatch/parsing/projection/pagination and ran `GOCACHE=/private/tmp/adomi-gocache go test -count=1 ./internal/ado ./internal/cli -run 'PullRequest|PRList|AgentSkill'` on the host: both packages passed. Sandbox attempts were blocked by cache access and loopback binding, not test assertions; host rerun resolved those restrictions.
- Task 2.2: branch guidance generator test observed RED then GREEN via `GOCACHE=/private/tmp/adomi-gocache go test ./internal/cli -run '^TestAgentSkillCreatesGlobalDefaultAdomiSkill$' -count=1`; README/reference updated. Joint gate 2.3 and final documentation review pending.
- Runtime PR discovery: temporary built binary with host execution and `--profile vetz --global --repository One.Backend --source feature/reflab-profilpakete/52127-k8s --status all` returned active PR 26881, target `refs/heads/develop`, with normalized source and UTC creation timestamp. Canonical fetch cross-check pending.
- Runtime PR cross-check: existing `pr fetch 26881 --profile vetz --global --json` in `/private/tmp/adomi-investigation.O1Z6Pr` succeeded; exported `pull-request.json` matches the discovery ID/title/status/source/target/creation timestamp. This satisfies the PR portion of gate 6.3 for the current implementation.

- Gate 2.3: root reviewed query propagation, source validation, parsing and HTTP-to-CLI fixtures; `GOCACHE=/private/tmp/adomi-gocache go test -count=1 ./internal/ado ./internal/cli -run 'Pipeline|AgentSkill'` passed on the host for both packages.

- Task 3.3: generated comment guidance test observed RED then GREEN; `GOCACHE=/private/tmp/adomi-gocache go test ./internal/cli -run '^TestAgentSkillCreatesGlobalDefaultAdomiSkill$' -count=1` passed. README/reference include opt-in scope, metadata, files, limits and unchanged stdout/counts. Help remains part of task 3.2 integration; joint gate 3.4 pending.
- Runtime branch history: host temporary binary `ado pipeline list --branch feature/reflab-profilpakete/52127-k8s --last 10 --profile vetz --global` returned ten matching runs including Friday 2026-08-28 deployment 361410 at source SHA `b14c8a9f0293f8f2257ee55d64c75ad358a6e40e`. This reaches the historical deployment missed by the project-wide window. Existing `pipeline get 361410` independently matched run identity, branch, SHA, status and timestamps.

- Task 3.1: Luna reader implementation and HTTP identity/deleted-record/pagination/limit fixtures passed; root reviewed separate read model and endpoint, configured-token URLs, null metadata, and duplicate tracking including deleted entries. Existing creation code was unchanged.
- Runtime comments: host temporary binary `ado fetch 56445 --include-comments --profile vetz --global --json` in the temporary Git repository succeeded with exactly `{path,workItems,attachments}` (3 items, 0 attachments). Export includes root 56445 (3 comments), parent 52127 (2), Epic 52124 (explicit empty array), and matching index references. Comment 53144 by Albert Frischmann retains the current text `<div>Gggf. nicht nötig. </div>` and version 1, matching the handover context identified by the user. Parent comment 52053 retains related usage/aggregation context. No ADO mutations occurred.

- Gate 3.4: root ran `GOCACHE=/private/tmp/adomi-gocache go test -count=1 ./internal/ado ./internal/cli -run 'WorkItem|Fetch|Export|AgentSkill'` on the host after typed HTML renderer and enriched-to-default refresh coverage: both packages passed. Root verified comment reads precede exporter invocation and use separate feedback; renderer escapes typed text/author/dates and default index uses omission.

- Runtime cross-check limitation: computer-use reported no browser available for the selected run. Automatic approval review rejected a separate raw-API baseline script because it would retrieve the PAT directly through macOS `security`, outside Adomi authentication. The rejected script was not executed and was removed; no bypass attempted. Authorized host Adomi reads remain available. Independent UI/raw baseline comparison remains unavailable unless the user accepts deterministic and Adomi-native evidence or supplies an approved verification path.

- Reader grounding during task 4.2: root checked Microsoft Test Runs/Results contracts and corrected the in-progress implementation to use documented `build`/`buildConfiguration` identity, string-or-number shallow-reference IDs, result IDs scoped per test run, and calculated outcome counts separate from reported totals. Design now records these concrete wire details; approved behavior/scope is unchanged.

- Task 5.3 guidance drafted alongside reader implementation: generator test first failed on missing inspection guidance, then README/reference/generated guidance were updated for scope, permissions, artifacts, counts, limits and failure semantics. The immediate rerun encountered concurrent reader compilation edits; generator/help verification is deferred to gate 5.4 after integration stabilizes.

- Task 4.2: Luna completed HTTP-backed Test run/result paging with documented identity shapes, run-scoped result IDs, reported totals/calculated outcome counts, and failure fixtures. Luna full ADO tests/vet passed; root reviewed corrected wire mapping and per-run duplicate handling. Joint reader gate 4.3 remains pending task 4.1.

- Gate 4.3: root ran `GOCACHE=/private/tmp/adomi-gocache go test -count=1 ./internal/ado -run 'Pipeline|Inspection'` on the host: passed. Root verified UUID record/parent/attempt references, requested timeline identity, aggregate record budget, shared request/body accounting, text/plain after authorization, fixed configured URLs, run-scoped test identities and separate calculated outcomes. Existing summary/pipeline transport regressions remained green.

- Task 5.3 generator follow-up after reader stabilization: `GOCACHE=/private/tmp/adomi-gocache go test ./internal/cli -run '^TestAgentSkillCreatesGlobalDefaultAdomiSkill$' -count=1` passed. Guidance now distinguishes summary status from inspection evidence and preserves all PR governance/host execution guidance. Help integration and joint gate 5.4 remain pending.

- Task 5.1: Luna snapshot exporter tests passed, including real injected `os.ErrPermission` after an earlier artifact write. Root caught and verified the fix for named-return cleanup losing the snapshot path. Export captures the directory independently, writes index last, retains older snapshots, emits explicit empty arrays and timeline-scoped log references. Joint gate 5.4 remains pending CLI handoff.

- Gate 5.4: root `GOCACHE=/private/tmp/adomi-gocache go test -count=1 ./internal/ado ./internal/cli -run 'Pipeline|Inspection|AgentSkill'` passed for both packages. HTTP-to-CLI-to-filesystem fixtures verify actual failed task/log/result content, exact JSON/plain stdout and safe failure behavior; typed summary JSON is shared with unchanged list/get.
- Runtime inspection: host temporary binary `ado pipeline inspect 363857 --profile vetz --global --json` succeeded: 14 records (including Checkpoint/Phase/Stage/Job/Task), one failed-task log, 66 test runs, 5,492 results. `run.json` exactly matched separately captured existing `pipeline get 363857` JSON. All per-run calculated counts match exported results; aggregate outcomes are Passed 5,323 / Failed 166 / NotExecuted 3. Log 10 (3,407,581 bytes, SHA256 `341907646e21b3e16e4d04384076172c9f283ff74074077b86d70b9cd1f6f522`) maps to failed Dotnet Test task and contains the same failed test name/category and 166 failed / 5,323 passed totals. Parsed console summaries include only one skipped test; published results contain three NotExecuted records, so these sources remain separately labeled rather than forced equal. Index metadata/counts/path references verified.
- Runtime deployment inspection: `ado pipeline inspect 361410 --profile vetz --global` printed only a snapshot path on stdout and exported 52 timeline records, no failed-task logs and zero published test runs/results. This tests the successful empty-published-test case without inferring that tests did not execute. The verification uses existing Adomi authentication and cross-correlates separate Build summary, timeline, log and Test endpoint evidence; independent browser/raw-script comparison remains unavailable as recorded above.

- Gate 6.1: independent implementation-review examined tracked and untracked final artifacts and returned zero findings, correct verdict (0.89 confidence). Root verified material wire contracts, export cleanup, output semantics and governance compatibility; no review fixes remained.
- Gate 6.2: after review, root host checks `GOCACHE=/private/tmp/adomi-gocache go test -count=1 ./...`, `go vet ./...`, and `go build ./...` all passed across the module. `gofmt -l` returned no filenames for all 40 changed/new Go files. `openspec validate --all --strict --no-interactive` passed 10/10, zero failures; `git diff --check` passed.
- Gate 6.3 disposition: satisfied through successful host Adomi reads for all four new behaviors, canonical existing PR/Build summary comparisons, and cross-correlation of separately retrieved timeline/log/Test evidence. No live case was missing: failed task plus full log and published outcomes were verified on 363857, and successful empty published tests on 361410. Independent browser/raw-script comparison was unavailable; the rejected credential-reading script was not executed and no alternate direct-credential access was attempted. Friday deployment 361410 retained one skipped Checkout task and one failed nested Build record despite overall success, without downloading non-task logs or inventing outcomes.
- Gate 6.4 compatibility portion: compared shared requirement blocks with completed `enable-complete-pr-flow`; all five scenarios in the replacing workflow-documentation requirement are retained, generated investigation guidance is a distinct added requirement, and governance/host-keyring instructions remain intact. Future archive order remains predecessor first; neither change is archived. Final project log pending.

- Gate 6.4 complete: appended the implementation session to `1.Projects/adomi/1. Daily Work/2026/09/2026-09-07.md` using host Obsidian CLI and read back the exact path/content. All 22 tasks are complete. Changes remain local/uncommitted; no install, archive, publication or ADO mutation.
