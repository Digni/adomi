## 1. Add failing contract tests

- [x] 1.1 Add CLI tests for `adomi ado pr link` covering one and repeated `--work-item` flags, input-order preservation, duplicate/invalid/missing ID rejection before client creation, unknown arguments, plain output, JSON output, and empty stdout on failure. Verification (Immediate): run `go test ./internal/cli -run 'TestADOPullRequestLink'`; expected result before implementation is failure only because the link command/behavior is missing.
- [x] 1.2 Add CLI orchestration tests for full preflight before writes, exact already-linked detection, mixed new/existing items, missing PR project/repository identity, missing/non-positive work-item revision, revision conflict, mismatched update response, stop-on-first-write-failure, prior-linked IDs in the error, and no automatic retry. Verification (Immediate): run `go test ./internal/cli -run 'TestADOPullRequestLink'`; expected result before implementation is failure at the missing orchestration seams rather than unrelated existing behavior.
- [x] 1.3 Add Azure DevOps client tests for the exact `%2F` PR artifact URI, `PATCH` work-item URL with expanded relations, configured API version, Basic auth, `application/json-patch+json`, `/rev` test plus `ArtifactLink` body, strict response decoding, response ID/revision/relation validation, redirects, network failures, and non-2xx errors. Verification (Immediate): run `go test ./internal/ado -run 'TestClientLinkWorkItemToPullRequest'`; expected result before implementation is failure because the client method is absent.
- [x] 1.4 Add failing help and generated-skill assertions for `pr link`, repeatable work-item flags, output/failure semantics, work-item write permission, idempotent re-runs, non-atomic batches, and the narrowed “no generic relation editing” boundary. Verification (Immediate): run `go test ./internal/cli -run 'TestADO.*Help|TestAgentSkill'`; expected result before implementation is failure only for the new documented contract.

## 2. Implement PR and work-item client support

- [x] 2.1 Extend the decoded PR/work-item models with access to the PR project GUID and work-item revision while preserving existing JSON/export metadata; add focused helpers for constructing the encoded PR artifact URI and detecting its exact `ArtifactLink` relation. Verification (Deferred): task 2.3's targeted client tests necessarily exercise decoded identities, revisions, exact URI construction, and relation detection.
- [x] 2.2 Extend the shared JSON request options with a content-type override that defaults to `application/json`, then add the narrow work-item link options/interface/client method using revision-tested `ArtifactLink` JSON Patch and existing auth/error/strict-decode behavior. Verification (Deferred): task 2.3's targeted client tests prove the new media type and request/response contract, while task 5.2's full suite proves the default content type remains unchanged for existing calls.
- [x] 2.3 Update affected fake clients/interfaces and make the Azure DevOps link contract tests pass without changing existing fetch/comment/PR maintenance behavior. Verification (Immediate): run `go test ./internal/ado -run 'TestClientLinkWorkItemToPullRequest|TestClientFetchWorkItem|TestClientCreateWorkItemComment|TestClient.*PullRequest'`; expected result is all selected tests pass.

## 3. Implement CLI parsing and orchestration

- [x] 3.1 Add `link` dispatch and argument parsing with positive PR/work-item IDs, at least one repeatable `--work-item`, duplicate rejection, `--profile`, `--global`, `--json`, and complete validation before configuration, credentials, or network access. Verification (Immediate): run `go test ./internal/cli -run 'TestADOPullRequestLink.*Parse|TestADOPullRequestLink.*Validation'`; expected result is all parser/early-failure tests pass.
- [x] 3.2 Implement link orchestration: create the maintenance client, fetch/validate the PR identity, preflight every work item, classify exact existing links, update missing links sequentially with expected revisions, stop on the first failure, and report durable earlier links in the error. Verification (Immediate): run `go test ./internal/cli -run 'TestADOPullRequestLink'`; expected result is all link orchestration, idempotency, concurrency, and partial-failure tests pass.
- [x] 3.3 Buffer success output until completion; emit requested IDs one per line in input order for plain output and the specified compact linked/unchanged JSON result for `--json`, leaving stdout empty on every failure path. Verification (Immediate): run `go test ./internal/cli -run 'TestADOPullRequestLink.*Output|TestADOPullRequestLink.*Failure|TestADOPullRequestLink.*JSON'`; expected result is exact stdout/stderr contracts pass.

## 4. Update discoverability and documentation

- [x] 4.1 Add operation-specific `pr link` help and include `link` in PR namespace help without exposing unlink, generic relation editing, work-item field/state changes, or PR governance commands. Verification (Immediate): run `go test ./internal/cli -run 'TestADO.*Help|TestADOPullRequest.*Help'`; expected result is exact help tests pass.
- [x] 4.2 Update generated Adomi skill content and assertions with the link workflow, code-read/work-item-write permissions, idempotent re-runs, non-atomic multi-item behavior, data-only output, and the conservative boundary. Verification (Immediate): run `go test ./internal/cli -run 'TestAgentSkill'`; expected result is generated guidance tests pass.
- [x] 4.3 Update `docs/azure-devops.md` and relevant README command summaries with command examples, repeated flag behavior, output shapes, permissions, partial-failure warning, and scope exclusions. Verification (Immediate): run `rg -n 'ado pr link|work-item write|not atomic|generic relation' README.md docs/azure-devops.md internal/cli/agent.go`; expected result is consistent user/agent-facing coverage with no promise of unlink, cross-project, or unverified Azure DevOps Server behavior.

## 5. Review and final verification

- [x] 5.1 Run affected package verification after implementation. Verification (Immediate): run `gofmt -w` on changed Go files, then `go test ./internal/ado ./internal/cli`; expected result is formatting completes and both packages pass.
- [x] 5.2 Perform the standard-risk `implementation-review` over the complete diff, fix every actionable P0-P2 finding within scope, and rerun the affected test commands for any fix. Verification (Immediate): the review reports no unresolved actionable P0-P2 findings and all rerun affected tests pass.
- [x] 5.3 Run final repository gates after review fixes. Verification (Immediate): run `go test ./...`, `go vet ./...`, and `go build ./...`; expected result is the full test suite, static analysis, and full module build pass. If an unrelated environment/baseline failure blocks a gate, preserve its exact evidence and run the strongest affected checks without expanding scope.
- [x] 5.4 Validate artifacts and diff integrity. Verification (Immediate): run `openspec validate link-work-items-to-pull-requests --strict --no-interactive`, `openspec status --change link-work-items-to-pull-requests`, and `git diff --check`; expected result is a valid apply-ready change, no whitespace errors, and only planned changes plus any pre-existing unrelated work kept separate.
