## 1. Azure DevOps pipeline-run client

- [x] 1.1 Add failing `internal/ado` tests for `ListInProgressPipelineRuns` and `GetPipelineRun`, covering collection-base/project path escaping, `statusFilter=inProgress`, `queryOrder=queueTimeDescending`, configured API version, signed-int32 run-ID route boundaries, HTTPS and exact localhost/IPv4-loopback/IPv6-loopback transport acceptance, remote HTTP and non-HTTP rejection before request/authorization, bodyless GETs, Basic PAT authorization, representative Build response decoding, and exact lookup ID validation. Verification: `go test -count=1 ./internal/ado -run 'TestClient(ListInProgress|Get)PipelineRun'` fails because the pipeline-run client surface is absent.
- [x] 1.2 Add focused pipeline-run/definition/link models, define `PipelineRunReader` as returning only validated/normalized models or an error, and implement Build list/get URL construction, bodyless strict JSON requests with an 8 MiB per-response limit, stream-decoded run-ceiling enforcement before excess allocation, one reused-and-closed proxy-bypassed transport per loopback operation, required-shape validation, completed/non-completed status-result consistency, trimmed result normalization, missing/null/blank optional source-link normalization, UTC RFC3339Nano timestamp normalization, and consistency-preserving future status/result support in `internal/ado/pipeline.go`. Verification: `go test -count=1 ./internal/ado -run 'TestClient(ListInProgress|Get)PipelineRun'` passes for normal in-progress and completed responses, encoded collection/project paths, and configured API versions.
- [x] 1.3 Extend client tests for present null and empty-array list values, omitted top-level `value`, multi-page and empty-intermediate-page continuation, received-order preservation, a scripted state change between pages without restart/polling/reconciliation, duplicate IDs, absent/blank/whitespace-only/multiple/cyclic continuation headers, exact opaque-token forwarding without lexical interpretation, exactly-at-limit and over-limit behavior for the fixed 1,000-page and 100,000-run ceilings, pre-allocation rejection of excess run elements, no request beyond the page ceiling, direct loopback proxy bypass, exactly-8-MiB and declared/streamed-over-limit list/get responses, non-`inProgress` list items, 401/403/404/500 responses, authentication redirects, network errors, confidential diagnostic suppression using unique PAT/authorization/body/HTML/location markers, malformed/empty/multiple JSON values, ignored unrelated fields including the REST `url`, invalid date-times, UTC normalization of offset/fractional/whole-second timestamps, signed-int32 run/definition ID boundaries, missing/null/empty/whitespace/trimmed source-link inputs, missing required fields, mismatched get IDs, completed runs without results, non-completed runs with results, trimmed results, consistent unknown status/result values, and optional null output inputs. Preserve the generic nil-body `doJSON` behavior, success-response decoding behavior, and status-error diagnostics used by existing operations. Verification: `go test -count=1 ./internal/ado -run 'TestClient.*PipelineRun|TestClientPullRequestIterations|TestNewHTTPClient'` passes.

## 2. Pipeline CLI contract and wiring

- [x] 2.1 Add failing `internal/cli` tests for `adomi ado pipeline`, `pipeline list`, and `pipeline get <run-id>` discovery/help and parsing, including side-effect-free help, the repository requirement, run IDs at `1` and `2147483647`, missing/non-positive/non-decimal/`2147483648`/arbitrarily-large IDs, positional list arguments, missing values, repeated profile/global flags, unknown arguments, and early validation before config/credential/client access. Verification: `go test -count=1 ./internal/cli -run 'TestADOPipeline.*(Help|Reject|Require|Parse)'` fails before the command is registered.
- [x] 2.2 Embed `PipelineRunReader` in `ADOClient`, update the shared fake/default wiring, register the namespace/actions/help, add strict parsers and runner handlers, and project already-validated/normalized runs to the stable compact JSON DTO/envelope without duplicating Build-response validation. Add fake-dependency tests for profile/global/PAT/proxy/client propagation, ordered/non-null list output, empty `runs`, exact projection of normalized optional strings/timestamps/results, unchanged dependency-error propagation, and empty stdout for repository/config/keyring/client/fetch/reader-validation/encoding errors. Verification: `go test -count=1 ./internal/cli -run 'TestADOPipeline'` passes and existing `go test -count=1 ./internal/cli -run 'TestADO(Fetch|Wiki|PR|WorkItem)'` remains green after the shared interface change.
- [x] 2.3 Add production-wiring `httptest` coverage that exercises the real profile/PAT/proxy/client flow for a multi-page in-progress list and completed-run lookup, asserting encoded routes/queries, Basic auth, no GET body/content type, exact compact JSON keys/nulls/order, one trailing newline, and empty stderr on success. Add later-page failure, malformed completed run, HTTP status, and authentication-redirect cases that leave stdout empty and use unique markers to prove real client-produced errors do not leak PATs, authorization values, response bodies, HTML, or redirect locations; `main` prints those returned errors unchanged in production. Verification: `go test -count=1 ./internal/cli -run 'TestADOPipeline.*(Real|Redirect|Failure)'` passes.

## 3. Agent guidance and public documentation

- [x] 3.1 Update generated Adomi skill content and required-content assertions with both pipeline commands, the exact `inProgress` and best-effort one-shot rather than transactional snapshot semantics across YAML and classic Build pipelines, compact JSON result contract, `vso.build` scope, repository/profile and HTTPS-or-loopback transport behavior, and stage/job/environment/classic Release/mutation exclusions. Verification: `go test -count=1 ./internal/cli -run 'Test.*AdomiSkill'` passes and `rg -n 'adomi ado pipeline (list|get)' internal/cli/agent.go internal/cli/agent_test.go` finds both commands.
- [x] 3.2 Add concise pipeline status examples and scope notes, including the best-effort pagination and HTTPS-or-loopback transport semantics, to `README.md` and the command inventory in `docs/adomi_azure_devops_handover.md`, matching Cobra help and the generated skill. Verification: the following command exits zero and therefore finds every required term in every intended surface:
  ```sh
  for file in README.md docs/adomi_azure_devops_handover.md internal/cli/agent.go; do
    for term in 'pipeline list' 'pipeline get' 'vso.build' 'classic Release' 'best-effort' 'HTTPS' 'loopback'; do
      rg -q -F "$term" "$file" || exit 1
    done
  done
  ```

## 4. Verification and review gates

- [x] 4.1 Run formatting and focused regression suites after all implementation chunks. Verification: `gofmt -w internal/ado/*.go internal/cli/*.go`, `gofmt -l internal/ado/*.go internal/cli/*.go` prints nothing, and `go test -count=1 ./internal/ado ./internal/cli` passes.
- [x] 4.2 Run the full project and specification gates. Verification: `go test -count=1 ./...`, `go build ./...`, `go vet ./...`, `openspec validate check-ado-pipeline-runs --strict`, and `git diff --check` all exit zero.
- [x] 4.3 Run the `agent-review` multi-perspective implementation review, address every actionable finding within the approved scope, back-propagate any invalidated assumption across proposal/design/specs/tasks, and rerun all affected focused and full gates. Verification: final reviewer passes report no unresolved actionable findings and every command from 4.1-4.2 remains green.
