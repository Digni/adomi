# Tasks: list-recent-pipeline-runs

## 1. Azure DevOps recent-runs client

- [x] 1.1 Add failing `internal/ado` tests for `ListRecentPipelineRuns`, covering first-request `$top=N` and descending queue-time order, absent `statusFilter`, collection-base/project path escaping, configured API version, HTTPS and exact loopback transport acceptance inherited from the shared pipeline transport guard, bodyless GETs, Basic PAT authorization, mixed-status response decoding, and fewer-runs-than-requested success. *Verification: `go test -count=1 ./internal/ado -run 'TestClientListRecentPipelineRuns'` fails because the method is absent.*
- [x] 1.2 Add `ListRecentPipelineRuns(ctx context.Context, n int) ([]PipelineRun, error)` to `PipelineRunReader` and implement it in `internal/ado/pipeline.go`/`pipeline_transport.go` by sharing URL construction, transport validation, bodyless strict JSON GETs, continuation handling, and `normalizePipelineRun` with the in-progress path; parameterize the status filter and send a decrementing `$top` (N on the first request, N minus accumulated runs on each continuation request), stopping successfully once N runs are accumulated even if a continuation header remains, while keeping `ListInProgressPipelineRuns` byte-for-byte in behavior. Reject `n` outside `1..200` before any transport access. *Verification: the new tests from 1.1 pass and all existing `TestClientListInProgressPipelineRuns*` tests remain green unchanged.*
- [x] 1.3 Extend recent-runs client tests for continuation traversal below N with decrementing `$top` asserted per request, exactly-at-N success with an unconsumed continuation header (no further request), a single page exceeding its requested `$top` failing as a service-contract violation, invalid/blank/multiple/cyclic continuation tokens, duplicate run IDs across pages, present null and empty-array `value`, omitted top-level `value`, completed-without-result and non-completed-with-result consistency failures, unknown-but-consistent status/result preservation, trimmed results, timestamp normalization, 401/403/404/500 and redirect diagnostics with unique PAT/authorization/body/HTML/location markers suppressed, malformed/empty/multiple JSON values, and declared/streamed 8 MiB boundaries. *Verification: `go test -count=1 ./internal/ado` passes.*

## 2. CLI `--last` flag and wiring

- [x] 2.1 Add failing `internal/cli` tests for `pipeline list --last <N>` parsing: accepted boundaries `1` and `200`, missing value, flag-as-value, blank value, non-decimal, zero, negative, `201`, arbitrarily large values, repeated `--last`, positional arguments, repeated profile/global, unknown arguments, and early validation before repository/config/credential/client access. *Verification: `go test -count=1 ./internal/cli -run 'TestADOPipelineList.*(Reject|Accept|Parse)'` fails before the flag exists.*
- [x] 2.2 Add `--last` to the `pipeline list` parser, extend the shared fake with `ListRecentPipelineRuns`, wire the runner to call it when `--last` is present and the untouched in-progress path otherwise, and project through the existing DTO/envelope. Update namespace/list help text for both modes and the `1..200` range. *Verification: `go test -count=1 ./internal/cli -run 'TestADOPipeline'` passes with ordered/non-null recent-list output, empty `runs`, and unchanged dependency-error propagation and empty-stdout guarantees.*
- [x] 2.3 Add production-wiring `httptest` coverage exercising the real profile/PAT/proxy/client flow for a multi-page recent list returning mixed statuses, asserting encoded route/query (`$top`, `queryOrder`, absent `statusFilter`), Basic auth, exact compact JSON keys/nulls/order, one trailing newline, and empty stderr on success; plus failure cases (later-page failure, inconsistent completed run, HTTP status, redirect) that leave stdout empty and prove marker suppression. *Verification: `go test -count=1 ./internal/cli -run 'TestADOPipeline.*(Real|Redirect|Failure)'` passes.*

## 3. Agent skill and documentation

- [x] 3.1 Update generated Adomi skill content and required-content assertions with `--last <N>`, the `1..200` range, the project-wide most-recently-queued semantics across any status, client-side matching on `sourceBranch`/`sourceVersion`, the discovery-then-`get` workflow, and the unchanged exclusions. Update the `docs/azure-devops.md` "Pipeline runs" section, which currently documents `list` as exclusively `inProgress`, with both modes, the `1..200` range, and the unchanged one-shot semantics. Update the README pipeline row if it describes the list scope. *Verification: `go test -count=1 ./internal/cli -run 'Test.*AdomiSkill'` passes, `rg -n 'pipeline list --last' internal/cli/agent.go internal/cli/agent_test.go` finds the new flag, and the `docs/azure-devops.md` pipeline section shows both modes.*

## 4. Verification

- [x] 4.1 Run the full verification gate: `gofmt -l .` reports no files, `go vet ./...` passes, `go test -count=1 ./...` passes, `go build ./...` passes, `openspec validate list-recent-pipeline-runs --strict` passes, and `git diff --check` is clean. Then run the required `implementation-review` on the diff. *Verification: every gate passes; review findings resolved or dispositioned.*
