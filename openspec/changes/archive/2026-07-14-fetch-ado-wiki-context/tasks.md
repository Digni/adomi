## 1. Azure DevOps wiki client

- [x] 1.1 Add failing `internal/ado` tests for resolving a wiki by ID/name and fetching a page as strict JSON with `includeContent=true`, asserting base-path/project/wiki identifier/page-path query encoding, configured API version, Basic PAT authorization, and returned metadata/content. Verification: `go test -count=1 ./internal/ado -run 'TestClient(Resolve|Fetch)Wiki'` fails because the wiki client surface is absent.
- [x] 1.2 Add wiki/page models, the `WikiFetcher` interface, URL builders, and authenticated client methods that reuse the existing non-2xx/redirect and strict single-JSON-value error behavior. Verification: `go test -count=1 ./internal/ado -run 'TestClient(Resolve|Fetch)Wiki'` passes for successful responses, base URLs with collection paths, encoded names/paths, and configured API versions.
- [x] 1.3 Extend client regressions for bodyless wiki GETs, 401/404/500 responses, authentication redirects, network errors, malformed/empty/multiple JSON values, missing canonical wiki ID, missing/non-absolute/mismatched page paths, and empty page content without leaking PAT, HTML, or redirect locations. Preserve the existing shared nil-body `doJSON` wire behavior for pull-request iteration GETs. Verification: `go test -count=1 ./internal/ado -run 'TestClient.*Wiki'` passes and existing `go test -count=1 ./internal/ado -run 'Test(NewHTTPClient|ClientFetchWorkItem|ClientDownload|PullRequestMaintenanceMethods)'` remains green.

## 2. Wiki context orchestration

- [x] 2.1 Add failing orchestration tests for one-page fetches and recursive multi-level `subPages` trees, including stable path ordering, one content request per unique page, empty leaf lists/content, no descendant-content requests without `--recursive`, recursive `/` selection, and rejection of absolute paths outside a non-root requested subtree. Verification: `go test -count=1 ./internal/ado -run 'TestFetchWikiContext'` fails before orchestration exists.
- [x] 2.2 Implement `FetchWikiContext` so it resolves the wiki, validates the canonical ID/requested absolute path, recursively flattens metadata, treats every absolute path as a descendant of `/`, rejects missing/non-absolute/duplicate/out-of-subtree paths and response/request path mismatches, fetches content sequentially, and returns a complete in-memory context before export. Verification: `go test -count=1 ./internal/ado -run 'TestFetchWikiContext'` passes for single, recursive, recursive-root, nested, empty-content, duplicate, missing-shape, mismatched-path, out-of-subtree, and mid-fetch error cases.

## 3. Repository-local wiki export

- [x] 3.1 Add failing export tests for `.adomi/context/wikis/<safe-wiki-id>/`, `wiki.json`, ordered `pages.json`, fixed-length SHA-256 page Markdown filenames (including long valid paths), path-to-file `index.json` entries, profile/project/selection/timestamp metadata, empty Markdown, slash-normalized relative paths, existing symlinks in the bundle path below the repository root, and an atomic final-index success marker. Verification: `go test -count=1 ./internal/ado -run 'Test(WikiOutputPath|ExportWikiContext)'` fails before export support exists.
- [x] 3.2 Implement safe wiki-ID encoding, bounded SHA-256 page filenames, containment/collision and symlink-boundary validation, stable ordering, stale-bundle replacement, Markdown/JSON writes, and atomic temp-file-plus-rename final-index behavior using existing export helpers where appropriate. Verification: `go test -count=1 ./internal/ado -run 'Test(WikiOutputPath|ExportWikiContext)'` passes for normal, long-valid, traversal-like, platform-unsafe, duplicate/collision, symlinked-destination, stale-file, zero-time, nil/invalid-context, atomic-index, and filesystem-failure cases; failed exports return an empty path and no regular final `index.json`.

## 4. CLI command and production wiring

- [x] 4.1 Add failing CLI parser/help tests for `adomi ado wiki fetch <wiki-id-or-name> --page <absolute-path> [--recursive] [--profile <name>] [--global]`, including both help levels, namespace discovery, missing/blank/non-absolute paths, missing/blank wiki identifiers, rejection of every repeated flag, value-less/unknown flags, early validation, and the repository requirement under global scope. Verification: `go test -count=1 ./internal/cli -run 'TestADOWiki.*(Help|Reject|Require|Parse)'` fails before the command is registered.
- [x] 4.2 Extend `ADOClient`/`Dependencies`, default and fallback wiring, command construction/help, argument parsing, and the runner flow. Add fake-dependency tests proving profile/PAT/proxy/config propagation, single versus recursive selection, export options, path-only success stdout, and empty stdout for configuration, credential, client, fetch, and export failures. Verification: `go test -count=1 ./internal/cli -run 'TestADOWiki'` passes.
- [x] 4.3 Add an `httptest` real-production-wiring test that resolves a wiki, returns a multi-level page tree/content, exports the bundle, and asserts authenticated encoded requests and stdout. Add real-wiring failures for a page error and authentication redirect, asserting no success index/stdout and no HTML/Location/PAT leakage. Verification: `go test -count=1 ./internal/cli -run 'TestADOWiki.*Real|TestADOWiki.*Redirect|TestADOWiki.*Failure'` passes.

## 5. Documentation and completion gates

- [x] 5.1 Update `README.md` and generated Adomi agent-skill content/tests with the exact wiki fetch command, repository-local bundle purpose, recursive opt-in, and attachment/search exclusions. Verification: `go test -count=1 ./internal/cli -run 'Test.*AdomiSkill|TestADOWiki.*Help'` passes and `rg -n 'adomi ado wiki fetch' README.md internal/cli/agent.go internal/cli/agent_test.go` finds the documented command in all three locations.
- [x] 5.2 Run formatting and focused regression suites. Verification: `gofmt -w internal/ado internal/cli`, `gofmt -d internal/ado internal/cli` prints nothing, and `go test -count=1 ./internal/ado ./internal/cli` passes.
- [x] 5.3 Run full project verification and strict OpenSpec validation. Verification: `go test -count=1 ./...`, `go build ./...`, `openspec validate fetch-ado-wiki-context --strict`, and `git diff --check` all exit zero.
- [x] 5.4 Run the `agent-review` multi-perspective implementation review, address every actionable finding within approved scope, back-propagate any invalidated assumptions to proposal/design/specs/tasks, and rerun affected focused plus full gates. Verification: final reviewer passes report no unresolved actionable findings and all commands from 5.2-5.3 remain green.
