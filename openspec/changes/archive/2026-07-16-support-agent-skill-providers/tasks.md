## 1. Provider Contract and CLI Implementation

- [x] 1.1 Add table-driven tests for all six global and project provider mappings, default behavior, `--claude` compatibility, unknown providers, selector conflicts, help text, empty stdout on validation failure, and unchanged replacement behavior. **Verification: Immediate** — run `go test ./internal/cli -run 'TestAgentSkill'`; the new provider cases must fail before implementation for the expected missing behavior. **Actual:** observed RED failures for the missing flag, Claude target, pre-path provider validation, selector conflict, and help matrix; the completed focused suite passed.
- [x] 1.2 Implement `--provider`, supported-provider validation, the shared-versus-Claude target-family resolver, and quiet `--claude` compatibility without changing generated skill content or write safety. **Verification: Immediate** — run `go test ./internal/cli`; all provider, help, stream, and existing agent-skill tests must pass. **Actual:** `go test ./internal/cli -count=1` passed.

## 2. User Documentation

- [x] 2.1 Update `README.md`, `docs/getting-started.md`, and `docs/azure-devops.md` to name Codex, OpenCode, Pi, GitHub Copilot, Cursor, and Claude; show `--provider`; explain the shared `.agents/skills` installation and Claude-specific root; preserve `--claude`, scope, and replacement guidance; and link only current primary provider documentation where compatibility is claimed. **Verification: Immediate** — run `go run ./cmd/adomi agent skill --help` and compare every documented provider, flag, and target path against the command output and provider matrix, then run `git diff --check`; the documentation and CLI must agree with no whitespace errors. **Actual:** live help and all documented provider/path mappings agree; the documented remote `go install github.com/Digni/adomi/cmd/adomi@latest` command installed a runnable binary in an isolated `GOBIN`; `git diff --check` passed.

## 3. Review and Final Verification

- [x] 3.1 Format the Go changes and run the pre-review repository checks. **Verification: Immediate** — run `gofmt -w internal/cli/agent.go internal/cli/agent_test.go`, `go test ./...`, and `go build ./...`; all commands must pass. **Actual:** formatting completed, `go test ./...` passed all six packages, and `go build ./...` passed after the sandbox-blocked Go-cache access was rerun with the required broader access.
- [x] 3.2 Run the standard-risk `implementation-review` workflow over the complete change and resolve every actionable P0-P2 finding within scope. **Verification: Immediate** — the final review must report no remaining actionable P0-P2 findings. **Actual:** focused review returned `patch is correct` at 98% confidence with no actionable P0-P2 findings; independent validation agreed.
- [x] 3.3 Re-run the post-review merge gate and validate the durable contracts. **Verification: Immediate** — run `gofmt -l cmd internal`, `go test ./...`, `go build ./...`, a relative Markdown link check for `README.md` and maintained guides, `git diff --check`, and `openspec validate support-agent-skill-providers --strict`; formatting must be clean, links must resolve, and every command must pass. **Actual:** formatting was clean; all six packages passed; the full build passed; all 14 relative Markdown links resolved; `git diff --check` passed; the change and all eight canonical/change OpenSpec items passed strict validation.
