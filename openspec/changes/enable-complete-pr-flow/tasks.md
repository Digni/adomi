## 1. Azure DevOps governance transport

- [x] 1.1 Add failing `internal/ado` HTTP contract tests for typed and presence-aware PR governance fields, authenticated-user connection data, lifecycle PATCH bodies for complete/auto-complete/cancel/abandon, legacy squash normalization, safe completion-option preservation, source-commit pinning, explicit bypass/optional-ignore clearing, and malformed response IDs/states; verify each focused tracer bullet fails for the missing behavior before its production edit (Immediate RED: `go test ./internal/ado -run 'TestClient(FetchPullRequestGovernanceFields|FetchAuthenticatedIdentity|UpdatePullRequestGovernance)'`).
<!-- Verification 2026-08-25: governance-field, identity, completion, auto-complete, and legacy-squash tracer bullets each failed for their missing type or method before its production edit. -->
- [x] 1.2 Implement the typed identity, commit, completion-option, reviewer, connection-data, and tri-state auto-complete models plus lifecycle PATCH and authenticated-user lookup in `internal/ado`; verify the focused tests from 1.1 pass and existing PR client tests remain green (Immediate GREEN: `go test ./internal/ado -run 'TestClient(FetchPullRequest|FetchAuthenticatedIdentity|UpdatePullRequest)'`).
<!-- Verification 2026-08-25: both focused commands passed; existing fetch/update client tests remained green. -->
- [x] 1.3 Add failing reviewer-vote HTTP contract tests for the repository-scoped reviewer PUT URL, authenticated reviewer ID, votes `10`, `5`, and `-10`, required-reviewer preservation inputs, non-required caller creation, authorization, and mismatched response identity/vote; verify each focused tracer bullet fails before its production edit (Immediate RED: `go test ./internal/ado -run 'TestClientSetPullRequestReviewerVote'`).
<!-- Verification 2026-08-25: the initial reviewer-vote tracer failed because the method/options were absent; the completed suite passed for all supported votes, exact wire shape, invalid input, and mismatched responses. -->
- [x] 1.4 Implement the authenticated-user reviewer-vote transport and narrow `PullRequestGovernor` interface, with exact response validation and no arbitrary reviewer selector; verify all Azure DevOps package tests pass (Immediate GREEN: `go test ./internal/ado`).
<!-- Verification 2026-08-25: go test ./internal/ado passed. -->

## 2. PR command state machine and output

- [x] 2.1 Add failing table-driven CLI tests for all seven verbs, positive-ID and flag validation before credentials, presence-aware completion preferences, profile/global selection, every allowed/idempotent/rejected PR state, missing repository/commit/identity data, source races, setter clearing, required-reviewer preservation, no unintended extra writes, and plain/JSON/error stream contracts; verify the focused governance suite is red before implementation (Immediate RED: `go test ./internal/cli -run 'TestRunADOPullRequestGovernance'`).
<!-- Verification 2026-08-25: the focused suite failed because every new verb still fell through to numeric PR fetch parsing before the dispatcher and governance state machine existed. -->
- [x] 2.2 Implement `internal/cli/ado_pr_governance.go`, extend the existing PR dispatcher and client test fake, and wire complete, auto-complete, cancel-auto-complete, abandon, approve, approve-with-suggestions, and reject through one preflight-driven state machine; verify the governance tests from 2.1 pass (Immediate GREEN: `go test ./internal/cli -run 'TestRunADOPullRequestGovernance'`).
<!-- Verification 2026-08-25: the focused governance suite passed after implementing all seven verbs, presence-aware safe preferences, state/idempotency gates, authenticated voting, and exact response proof. -->
- [x] 2.3 Add failing namespace and per-command help tests that prove every new verb and flag is discoverable, describes its state/policy/authenticated-user boundary, and performs no config, credential, or network access; then implement help routing/text and verify the entire PR help suite passes (Immediate RED/GREEN: `go test ./internal/cli -run 'TestADOPullRequest.*Help|TestRunADOPullRequest.*Help'`).
<!-- Verification 2026-08-25: the help tracer first failed on absent namespace entries and fell through to argument validation; after routing/text implementation the focused namespace, per-verb, and side-effect-free suite passed. -->
- [x] 2.4 Run the full CLI package tests to catch compatibility regressions in the numeric fetch alias and existing fetch, ensure, link, comment, reply, resolve, and reopen dispatch paths; fix only change-introduced failures (Immediate: `go test ./internal/cli`).
<!-- Verification 2026-08-25: the full internal/cli package suite passed without compatibility fixes. -->

## 3. Agent guidance and public documentation

- [x] 3.1 Add failing generated-skill tests for every new command, completion preferences, code write permission, current-state inspection, exact user-authorization requirement, asynchronous completion behavior, authenticated-user-only voting, cancellation, and remaining exclusions; verify the focused tests are red before changing the template (Immediate RED: `go test ./internal/cli -run 'Test.*AgentSkill'`).
<!-- Verification 2026-08-25: delegated vertical TDD recorded red failures for missing command, permission/authorization, completion-semantics, and lifecycle wording before the template edit. -->
- [x] 3.2 Update the generated Adomi skill content in `internal/cli/agent.go` and verify the focused generated-skill tests pass without weakening PAT handling or existing PR maintenance guidance (Immediate GREEN: `go test ./internal/cli -run 'Test.*AgentSkill'`).
<!-- Verification 2026-08-25: independently reran the focused generated-skill suite; it passed with existing PAT secrecy and PR maintenance assertions intact. -->
- [x] 3.3 Update `README.md` and `docs/azure-devops.md` with the exact lifecycle/vote syntax, flags, state restrictions, idempotent and async behavior, code write scope, stdout/JSON contract, explicit agent authorization, and unsupported governance boundary; verify every documented command against local `adomi ado pr` help and verify repository documentation links still resolve (Immediate: run the repository's documentation/link checks if present, plus the built-binary help smoke in 4.1).
<!-- Verification 2026-08-25: exact-case relative-link audit and diff whitespace checks passed; no repository documentation checker exists. Built-binary help comparison remains covered by 4.1. -->

## 4. Executable and live behavior verification

- [x] 4.1 Build an isolated Adomi binary and smoke `adomi ado pr --help` plus `--help` for all seven new verbs, verifying exact syntax and that help remains side-effect free (Immediate: `go build -o /tmp/adomi-pr-flow ./cmd/adomi`, then run the eight help commands against `/tmp/adomi-pr-flow`).
<!-- Verification 2026-08-25: isolated build passed; namespace and all seven help commands exited zero, wrote zero stdout bytes, and exposed their exact command usage from the built binary. -->
- [x] 4.2 After obtaining explicit user authorization and designated disposable Azure DevOps PRs, verify JSON-mode approval, approval-with-suggestions, rejection, auto-complete scheduling, auto-complete cancellation, abandonment, and immediate completion against the real service; read back each resulting state, ensure scheduling clears any seeded policy-bypass and optional-policy-ignore settings, ensure cancellation removes the setter object, and use a separate disposable PR to advance the source after preflight but before PATCH and prove Azure DevOps rejects completion pinned to the stale SHA. Retain sanitized command/result evidence without PATs. If no safe live environment is authorized, preserve that blocker and leave this task unchecked rather than claiming runtime verification (Immediate live gate).
<!-- Partial live verification 2026-08-25: with explicit user authorization, the locally built binary scheduled PR 26562 through the vetz profile with `--merge-strategy squash --json`; Azure DevOps returned action `auto-complete`, status `active`, merge status `succeeded`, and auto-complete enabled. An independent fetch confirmed an authenticated setter, squash strategy, preserved source-branch deletion, no stored bypass setting or reason, and removal of the previously stored optional-policy-ignore ID 325. The remaining vote, cancellation, abandonment, immediate-completion, and stale-source cases were not authorized and remain unverified. -->
<!-- Manual acceptance 2026-08-25: the user explicitly accepted this task based on the successful representative end-to-end auto-complete flow. Approval, approval-with-suggestions, rejection, cancellation, abandonment, immediate completion, and the stale-source race were not exercised live; their acceptance relies on the green HTTP contracts, CLI state-machine tests, and completed high-risk review. This checkbox does not claim live evidence for those omitted cases. -->
- [x] 4.3 If the live API contradicts the empty-identity cancellation encoding, immediate-vs-scheduled response assumptions, vote response shape, or another material contract, stop implementation and back-propagate the corrected decision through proposal, specs, design, and tasks before resuming; otherwise record that no artifact correction was required (Immediate disposition from 4.2).
<!-- Disposition 2026-08-25: the observed auto-complete scheduling response and independent read-back matched the design, including active scheduled state and policy-ignore sanitization, so no artifact correction was required. The cancellation encoding, vote response, immediate-completion response, and stale-source enforcement were not observed live and are manually accepted on contract-test and review evidence rather than represented as verified service behavior. -->

## 5. High-risk review and final repository gate

- [x] 5.1 Run the `agent-review` high-risk multi-perspective workflow over the complete implementation, focusing on irreversible completion, state/source races, policy-bypass leakage, authenticated identity, reviewer preservation, response proof, stdout safety, and compatibility; address all confirmed findings and rerun their targeted regression tests (Immediate review gate).
<!-- Verification 2026-08-25: multi-perspective review found inherited policy overrides, lossy reviewer export, weak cancellation/setter/required-reviewer proof, one insufficient mutation-count assertion, and stale help wording. Each was fixed with targeted green regressions; refreshed conformance, security/style, architecture, adversarial, plan-adherence, and test-coverage lenses reported no actionable findings. -->
- [x] 5.2 After review fixes, run repository-wide formatting, tests, static analysis, build, OpenSpec strict validation, and whitespace checks; require `gofmt -l` to return no changed Go files and all commands to pass before claiming merge-intended completion (Deferred final gate proving all implementation chunks: `gofmt -l .`, `go test ./...`, `go vet ./...`, `go build ./...`, `openspec validate enable-complete-pr-flow --strict --no-interactive`, and `git diff --check`).
<!-- Verification 2026-08-25: gofmt reported no files; go test ./..., go vet ./..., go build ./..., strict OpenSpec validation, and git diff --check all exited zero. The build emitted only a sandbox-denied Go module stat-cache write warning after succeeding. -->
